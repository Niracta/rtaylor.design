<article class="markdown">
  <slot />
</article>

<style is:global>
  .markdown {
    max-width: min(1280px, 60ch);
    margin-inline: auto;
    padding-inline: 32px;

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
      font: 700 var(--fs-heading) / 1.3 var(--family-body);
      margin-block: calc(var(--fs-heading) / 1.125)
        calc(var(--fs-heading) * 0.25);
      text-wrap: pretty;

      a {
        color: var(--colors-black);
        text-decoration: none;

        &:hover {
          text-decoration: underline;
        }

        &:visited {
          color: initial;
        }
      }
    }

    h2 {
      --fs-heading: var(--hh);
      font-weight: 600;
    }

    h3 {
      --fs-heading: var(--hl);
      font-weight: 600;
    }

    h4 {
      --fs-heading: var(--hm);
      font-weight: 500;
    }

    h5 {
      --fs-heading: var(--hs);
      font-weight: 500;
    }

    h6 {
      --fs-heading: var(--ht);
      font-weight: 500;
    }

    p,
    ul,
    ol,
    dl,
    section > img,
    iframe,
    figure,
    .split {
      margin-block: calc(var(--fs-body) / 1.125) calc(var(--fs-body) * 0.25);
      text-wrap: pretty;
    }

    small {
      --fs-body: var(--fs-300);
    }

    strong {
      font-weight: 700;
    }

    blockquote {
      position: relative;
      max-width: 40ch;
      padding-left: 30px;
      margin: 45px 0;
      border-left: 5px solid var(--colors-secondary-normal);
      font-size: var(--fs-500);
      font-style: italic;
      text-wrap: pretty;

      &:before,
      &:after {
        position: absolute;
        display: inline-flex;
        height: 100%;
        width: 1ch;
        color: var(--neutral-a300);
        opacity: 0.15;
        font-size: 210px;
        user-select: none;
      }

      &:before {
        content: "“";
        justify-content: flex-start;
        top: -0px;
        left: 0;
        line-height: 0.75;
      }

      &:after {
        content: "”";
        justify-content: flex-end;
        align-items: flex-end;
        bottom: -32px;
        right: 0;
        line-height: 0;
      }

      cite {
        display: block;
        color: var(--neutral-700);
        font-size: var(--fs-200);
        font-style: normal;
        text-transform: uppercase;
      }
    }

    ul,
    ol {
      display: flex;
      flex-direction: column;
      padding: 0;
      margin: 0;
      margin-left: 1ch;
      list-style: none;

      li {
        flex: 1 0 auto;
        justify-content: flex-start;

        &:before {
          display: inline-flex;
          flex: 1 0 auto;
          width: 1ch;
          margin-right: 0.5ch;
          color: var(--colors-secondary-normal);
          font-weight: bold;
        }
      }
    }

    li {
      &:before {
        content: "•";
      }
    }

    ol {
      counter-reset: ordered-item;

      ol {
        margin-left: 3ch;
      }

      li {
        counter-increment: ordered-item;

        &:before {
          content: "." counters(ordered-item, ".");
          color: var(--colors-secondary-normal);
          font-family: var(--family-label);
          font-variant-numeric: tabular-nums;
          text-align: right;
          white-space: nowrap;
          direction: rtl;
        }
      }
    }

    dl {
      display: grid;
      grid-template-columns: min-content 1fr;
      gap: 1ch;
      padding-inline: 24px;

      dt {
        grid-column: 1;
        text-align: right;
      }

      dd {
        grid-column: 2;
      }
    }

    iframe {
      display: flex;
      width: 80%;
      height: unset;
      margin-inline: auto;
      aspect-ratio: 16/9;
    }

    figure {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: calc(18px * 1.25);
      width: 100%;
      max-width: 50em;
      margin-inline: auto;
    }

    figcaption {
      max-width: 40ch;
      margin-block: calc(var(--fs-label) / 1.125) calc(var(--fs-label) * 0.25);
      margin-inline: auto;
      color: var(--neutral-700);
      font-size: var(--fs-label);
      font-weight: 500;
      font-style: italic;
      text-align: center;
    }

    .split {
      display: flex;
      align-items: center;
      gap: 1ch;
      margin-inline: -32px;

      @media (max-width: 1024px) {
        flex-direction: column;
      }

      img {
        width: 100%;
        height: 100%;
      }
    }
  }
</style>
